apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-service
  namespace: angular-micro
spec:
  replicas: 1
  selector:
    matchLabels:
      app: product-service
  template:
    metadata:
      labels:
        app: product-service
    spec:
      serviceAccountName: product-service-sa
      containers:
        - name: product-service
          image: PLACEHOLDER_ECR_URI/angular-microservices/product-service:latest
          ports:
            - containerPort: 5200
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: "Production"
            - name: ASPNETCORE_URLS
              value: "http://+:5200"
            - name: ConnectionStrings__DefaultConnection
              valueFrom:
                secretKeyRef:
                  name: product-service-db
                  key: connection-string
          volumeMounts:
            - name: db-secret
              mountPath: /mnt/secrets-store
              readOnly: true
          readinessProbe:
            httpGet:
              path: /health
              port: 5200
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 5200
            initialDelaySeconds: 30
            periodSeconds: 20
      volumes:
        - name: db-secret
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: product-service-db
---
apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: angular-micro
spec:
  type: ClusterIP
  selector:
    app: product-service
  ports:
    - port: 5200
      targetPort: 5200
